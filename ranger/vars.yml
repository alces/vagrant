kafka_version: 2.12-2.0.0
kafka_dir: "/usr/local/kafka_{{ kafka_version }}"
kafka_conf_dir: "{{ kafka_dir }}/config"
kafka_jaas_conf: "{{ kafka_conf_dir }}/kafka_server_jaas.conf"
kafka_tgz: "/var/tmp/kafka-{{ kafka_version }}.tgz"
kerberos_realm: adgr.eu-west-1.compute.internal
kerberos_server: 10.0.14.190
maven_version: 3.3.3
maven_dir: "/usr/local/apache-maven-{{ maven_version }}"
maven_tgz: /var/tmp/maven.tgz
ranger_db_pass: S0meCrYptoGr4phicTh1ng
ranger_version: 2.0.0-SNAPSHOT
ranger_build_dir: /var/tmp/ranger
ranger_dir: "/usr/local/ranger-{{ ranger_version }}-admin"
ranger_kafka_dir: "/usr/local/ranger-{{ ranger_version }}-kafka-plugin"
ranger_target_dir: "{{ ranger_build_dir }}/target"
solr_version: 8.1.0
solr_dir: "/usr/local/solr-{{ solr_version }}"
solr_bin_dir: "{{ solr_dir }}/bin"
solr_tgz: /var/tmp/solr.tgz